package com.hjed.ottnavigator

import androidx.compose.ui.graphics.Color

// --- THEME COLOR PALETTE — unified with boot animation
val MotifBackground = Color(0xFF020508)       // Matches splash background exactly
val MotifSurface = Color(0xFF071323)          // Deep blue-black panel surface
val MotifAccent = Color(0xFF8B5CF6)           // Electric violet (splash orbital ticks + pulse)
val MotifFocusNeon = Color(0xFF22D3EE)        // Neon cyan (splash dots + title glow)
val MotifAccentMuted = Color(0x338B5CF6)      // Translucent violet active state
val MotifHighlightPlaying = Color(0x3310B981) // Emerald playing tint (complements cyan/violet)
val MotifInputSurface = Color(0xFF061020)     // Dark indigo input field (blends with surface)
val MotifDrawerGlass = Color(0xD6030A18)      // Deep navy drawer glass (splash background family)
