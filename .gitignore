# Keep this repository deployment-only. Everything is private by default.
*

# Public deployment configuration and static site.
!.gitignore
!index.html
!vercel.json
!update.json
!package.json

# Activation server required by /api/activate.
!api/
!api/**

# Public APK/update artifacts.
!stream/
!stream/**
